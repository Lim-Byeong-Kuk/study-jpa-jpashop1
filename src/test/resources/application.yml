spring:
#  datasource:
#    url: jdbc:h2:mem:test   #스프링부트는 별도의 설정이 없으면 메모리모드로 동작한다.
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create  # 스프링부트는 default가 create-drop
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true

logging.level:
  org.hibernate.SQL: debug
#  org.hibernate.type: trace #쿼리 파라미터관련 로그남기기 (스프링 부트 2.x, hibernate5)